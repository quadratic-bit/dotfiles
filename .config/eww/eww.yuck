(include "./variables.yuck")
(include "./widgets/music.yuck")
(include "./widgets/workspaces.yuck")
(include "./widgets/sysinfo.yuck")

(defwidget bar []
    (centerbox :orientation "h" :class "bg"
        (music)
        (workspaces)
        (sidestuff)
    )
)

(defwindow bar :monitor 0
               :stacking "bg"
               :exclusive true
               :windowtype "dock"
               :geometry (geometry :x "0%" :y "0%" :width "100%" :anchor "top center")
    (bar)
)
